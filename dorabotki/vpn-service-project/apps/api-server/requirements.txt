# ── Telegram-бот ───────────────────────────────────────────────────────────
aiogram==3.15.0

# ── Web-фреймворк (вебхуки, Mini App API, ссылка-подписка) ─────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ── База данных ────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
aiosqlite==0.20.0            # только для локального режима (LOCAL_MODE=true)

# ── Кэш, FSM, антифрод ─────────────────────────────────────────────────────
redis==5.2.1

# ── Конфигурация и валидация ───────────────────────────────────────────────
# ВНИМАНИЕ: aiogram 3.15 требует pydantic<2.10 — не поднимайте версию
# без одновременного обновления aiogram, иначе сборка образа упадёт
# на этапе pip install с ResolutionImpossible.
pydantic==2.9.2
pydantic-settings==2.7.0
pyyaml==6.0.2

# ── HTTP-клиент (Marzban, платёжки) ────────────────────────────────────────
httpx==0.28.1
tenacity==9.0.0                # ретраи при недоступности Marzban

# ── Планировщик воркеров ───────────────────────────────────────────────────
apscheduler==3.11.0

# ── CLI (добавление нод, сервисные команды) ────────────────────────────────
typer==0.15.1
# click 8.2+ сменил сигнатуру make_metavar и ломает справку typer,
# поэтому версия закреплена явно
click==8.1.8

# ── Ключи Reality (X25519) ─────────────────────────────────────────────────
cryptography==50.0.1

# ── Логи ───────────────────────────────────────────────────────────────────
structlog==24.4.0
